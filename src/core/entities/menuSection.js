export default class MenuSection {
    constructor(id, name, menuId, menuItems) {
        this.id = id;
        this.name = name;
        this.menuId = menuId;
        this.menuItems = menuItems;
    }
}